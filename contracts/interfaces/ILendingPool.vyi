# pragma version ~=0.4.0

# Factory calls
@external
def initialize(
    core_addr: address,
    reserve_addr: address,
    _oracle_signer: address,
    operator_addr: address,
):
    ...


# Views reads
@external
@view
def paused() -> bool:
    ...
