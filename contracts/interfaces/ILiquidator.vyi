# pragma version ~=0.4.0

# LendingPool calls
@external
def seize(
    loan_id: uint256,
    token_id: uint256,
    collateral_amount: uint256,
    condition_id: bytes32,
    principal: uint256,
    epoch_end: uint256,
):
    ...


# Views reads
@external
@view
def pending_count() -> uint256:
    ...
