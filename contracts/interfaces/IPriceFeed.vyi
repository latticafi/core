# pragma version ~=0.4.0

# Anyone submits signed price attestation
@external
def submit_price(
    condition_id: bytes32,
    price: uint256,
    timestamp: uint256,
    deadline: uint256,
    signature: Bytes[65],
):
    ...


# LendingPool + PoolCore + Views read
@external
@view
def get_price(condition_id: bytes32) -> (uint256, uint256):
    ...


# Views reads
@external
@view
def is_circuit_broken(condition_id: bytes32) -> bool:
    ...
